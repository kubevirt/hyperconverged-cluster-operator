#!/bin/bash

KUBEVIRT_VERSION="v1.8.2"
CDI_VERSION="v1.65.0"
NETWORK_ADDONS_VERSION="v0.102.0"
SSP_VERSION="v0.25.0"
TTO_VERSION="v0.5.0"
HPPO_VERSION="v0.25.1"
HPP_VERSION="v0.25.0"
AAQ_VERSION="v1.8.0-alpha.0"
INFLIGHT_OPERATIONS_VERSION="v0.0.6"
WASP_AGENT_VERSION="v1.2.1"
AIE_WEBHOOK_VERSION="v1.1.0"
IOMMUFD_DEVICE_PLUGIN_VERSION="v0.0.1"
MIGRATION_OPERATOR_VERSION="v0.2.0-rc.1"
MIGRATION_CONTROLLER_VERSION="v0.2.1"
VIRT_TEMPLATE_VERSION="v0.1.8"
AUTOPILOT_VERSION="v0.0.9"

NODE_DRIVER_REG_VERSION="v2.16.0"
LIVENESS_PROBE_VERSION="v2.18.0"
CSI_SNAPSHOT_VERSION="v8.5.0"
CSI_SIG_STORAGE_PROVISIONER_VERSION="v3.5.0"
KUBE_RBAC_PROXY_VERSION="4.15"

KUBEVIRT_CONSOLE_PLUGIN_VERSION="v4.22.0"
KUBEVIRT_CONSOLE_PROXY_VERSION="v0.0.11"

CONTAINER_REGISTRY="quay.io/kubevirt"

RELEASE_DELTA="${RELEASE_DELTA:-0}"

PACKAGE_DIR="./deploy/olm-catalog/community-kubevirt-hyperconverged"
CSV_VERSION=$(ls -d ${PACKAGE_DIR}/*/ | sort -rV | awk "NR==$((RELEASE_DELTA+1))" | cut -d '/' -f 5)
REPLACES_CSV_VERSION=$(ls -d ${PACKAGE_DIR}/*/ | sort -rV | awk "NR==$((RELEASE_DELTA+2))" | cut -d '/' -f 5)
