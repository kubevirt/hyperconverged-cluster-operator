apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../base

resources:
  - namespace.yaml

patchesStrategicMerge:
  - operator_group.yaml
  - subscription.yaml
  - hco_cr.yaml
