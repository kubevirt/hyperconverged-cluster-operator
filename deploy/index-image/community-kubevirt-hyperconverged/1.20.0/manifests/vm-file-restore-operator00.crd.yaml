---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: virtualmachinefilerestores.filerestore.kubevirt.io
spec:
  group: filerestore.kubevirt.io
  names:
    kind: VirtualMachineFileRestore
    listKind: VirtualMachineFileRestoreList
    plural: virtualmachinefilerestores
    shortNames:
    - vmfr
    - vmfrestore
    singular: virtualmachinefilerestore
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.target.name
      name: Target
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.restoredFilesCount
      name: Files
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VirtualMachineFileRestore is the Schema for the virtualmachinefilerestores
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VirtualMachineFileRestoreSpec defines the desired state of
              VirtualMachineFileRestore.
            properties:
              source:
                description: Source specifies where to restore files from (PVC, VolumeSnapshot,
                  or remote storage).
                properties:
                  pvc:
                    description: PVC specifies a PersistentVolumeClaim to restore
                      from.
                    properties:
                      name:
                        description: Name is the name of the PVC.
                        type: string
                      namespace:
                        description: Namespace is the namespace of the PVC. If empty,
                          defaults to the same namespace as the VirtualMachineFileRestore.
                        type: string
                    required:
                    - name
                    type: object
                  remote:
                    description: Remote specifies a remote storage location (e.g.,
                      S3) to restore from.
                    properties:
                      bucket:
                        description: Bucket is the bucket name in the remote storage.
                        type: string
                      name:
                        description: Name is the name of the rclone remote (as configured
                          in the guest).
                        type: string
                    required:
                    - bucket
                    - name
                    type: object
                  snapshot:
                    description: Snapshot specifies a VolumeSnapshot to restore from.
                    properties:
                      name:
                        description: Name is the name of the VolumeSnapshot.
                        type: string
                      namespace:
                        description: Namespace is the namespace of the VolumeSnapshot.
                          If empty, defaults to the same namespace as the VirtualMachineFileRestore.
                        type: string
                    required:
                    - name
                    type: object
                type: object
              sourcePartition:
                description: SourcePartition specifies the partition number on the
                  backup volume to restore from.
                format: int32
                type: integer
              sourcePath:
                description: |-
                  SourcePath specifies the file or directory path to restore from the backup.
                  If not specified, manual restore mode is enabled (volume is hotplugged but no automatic restore).
                type: string
              target:
                description: Target is a reference to the target VirtualMachine to
                  restore files into.
                properties:
                  apiGroup:
                    description: |-
                      APIGroup is the group for the resource being referenced.
                      If APIGroup is not specified, the specified Kind must be in the core API group.
                      For any other third-party types, APIGroup is required.
                    type: string
                  kind:
                    description: Kind is the type of resource being referenced
                    type: string
                  name:
                    description: Name is the name of resource being referenced
                    type: string
                required:
                - kind
                - name
                type: object
                x-kubernetes-map-type: atomic
              targetPath:
                description: |-
                  TargetPath specifies where to restore files in the target VM filesystem.
                  If not specified, files are restored to their original locations.
                type: string
            required:
            - source
            - target
            type: object
          status:
            description: VirtualMachineFileRestoreStatus defines the observed state
              of VirtualMachineFileRestore.
            properties:
              attachmentRetries:
                description: |-
                  AttachmentRetries tracks the number of times we've waited for volume attachment.
                  Used to implement timeout for stuck attachments.
                format: int32
                type: integer
              completionTime:
                description: CompletionTime is the time when the restore operation
                  completed.
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the restore's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              errorMessage:
                description: ErrorMessage provides details about any error that occurred
                  during restoration.
                type: string
              lastAttachmentCheckTime:
                description: |-
                  LastAttachmentCheckTime is the timestamp of the last attachment status check.
                  Used for rate limiting to prevent rapid reconciliation loops.
                format: date-time
                type: string
              lastSSHCheckTime:
                description: |-
                  LastSSHCheckTime is the timestamp of the last SSH connection attempt.
                  Used for rate limiting to prevent rapid reconciliation loops.
                format: date-time
                type: string
              mountPath:
                description: |-
                  MountPath is where the restore volume is mounted in the guest OS.
                  For Linux: /backup, for Windows: C:\backup
                type: string
              phase:
                description: Phase represents the current phase of the file restore
                  operation.
                enum:
                - New
                - Init
                - Hotplugging
                - WaitingForAttachment
                - SSHConnecting
                - Restoring
                - VolumeReady
                - Cleanup
                - Succeeded
                - Failed
                type: string
              restoredFilesCount:
                description: RestoredFilesCount is the number of files successfully
                  restored.
                format: int32
                type: integer
              sshRetries:
                description: |-
                  SSHRetries tracks the number of SSH connection attempts.
                  Used to implement retry logic for transient SSH failures.
                format: int32
                type: integer
              startTime:
                description: StartTime is the time when the restore operation started.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
